# topcashback

Create framework for UI and API automation tests.
The structure follows the Page Object Model, where page classes store methods and selectors, while tests in the tests folder utilize these pages.

The task 1 (UI test):
Go to topcashback.com, capture the page title.
Login or Signup and capture the page title.
The test I created is authorization.spec.js

The task 2 (API test):
The test I created is api.spec.js

